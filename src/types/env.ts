export interface Env {
  host: string;

  infuraKey: string;
  formaticKey: string;
  portisId: string;
  walletconnectBridgeUrl?: string;
  coinbaseLink: string;

  gaTrackingId?: string;
  hotjarId?: string;
}
